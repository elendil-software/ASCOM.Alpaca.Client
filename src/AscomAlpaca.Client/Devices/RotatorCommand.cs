namespace ES.AscomAlpaca.Client.Devices
{
    internal enum RotatorCommand
    {
        CanReverse,
        IsMoving,
        Position,
        Reverse,
        StepSize,
        TargetPosition,
        Halt,
        Move,
        MoveAbsolute
    }
}
