namespace ES.Ascom.Alpaca.Client.Devices
{
    internal enum RotatorCommand
    {
        CanReverse,
        IsMoving,
        Position,
        Reverse,
        StepSize,
        TargetPosition,
        Halt,
        Move,
        MoveAbsolute
    }
}
